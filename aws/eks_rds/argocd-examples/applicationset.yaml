apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: guestbook
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - cluster: engineering
            url: https://kubernetes.default.svc
          - cluster: production
            url: https://kubernetes.default.svc
  template:
    metadata:
      name: "{{cluster}}-guestbook"
    spec:
      project: default
      source:
        repoURL: https://github.com/edsoncarlosdevops/personal-studies.git
        targetRevision: HEAD
        path: "aws/eks_rds/argocd-examples/apps/{{cluster}}"
      destination:
        server: "{{url}}"
        namespace: "guestbook-{{cluster}}"
