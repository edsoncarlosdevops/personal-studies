---
# ============================================================
# Windows Lab - Full Orchestration Playbook
# ============================================================
# Usage:
#   ansible-playbook site.yml                  # Full setup
#   ansible-playbook site.yml --tags security  # Only security
# ============================================================

- name: "Phase 1/3 - Baseline Configuration"
  ansible.builtin.import_playbook: playbooks/01-baseline.yml
  tags: [always, baseline]

- name: "Phase 2/3 - Security Hardening"
  ansible.builtin.import_playbook: playbooks/02-security-hardening.yml
  tags: [security, hardening]

- name: "Phase 3/3 - Monitoring Setup"
  ansible.builtin.import_playbook: playbooks/03-monitoring.yml
  tags: [monitoring, observability]
