# OpenTelemetry Operator Helm Chart Values
replicaCount: ${otel_operator_replica_count}

manager:
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 64Mi

admissionWebhooks:
  enabled: true

kubeRBACProxy:
  enabled: true
